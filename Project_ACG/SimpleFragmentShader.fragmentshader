#version 330 core

// Output data
out vec4 fragColor;

// The input variable from the vertex shader (Attention! Same name and same type) 
//in vec4 vertexColor;  

uniform vec4 objectColor; //color from main

void main()
{
	// Output color = red
	//fragColor = vertexColor;
	//fragColor=vec4(0.0, 0.7, 0.0, 1.0); 
	
	//ex2
	fragColor = objectColor;
}